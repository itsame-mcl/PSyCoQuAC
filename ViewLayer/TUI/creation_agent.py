from asciimatics import Frame
from asciimatics import Layout

class FrameCreationAgent:

    def __init__(self, screen):
        self._screen =

    frame = Frame(screen, 80, 20, has_border=False)
    layout = Layout([1, 1, 1, 1])
    frame.add_layout(layout)